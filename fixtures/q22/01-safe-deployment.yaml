apiVersion: apps/v1
kind: Deployment
metadata:
  name: safe
spec:
  template:
    spec:
      containers:
      - name: app
        image: nginx:1.27-alpine
        envFrom:
        - secretRef:
            name: database
